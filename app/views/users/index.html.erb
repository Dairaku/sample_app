<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
<!--     railsは自動的に_user.html.erbを探す-->    
   <%= render  user %> 
  <% end %>

<!--   <%= render @users %>でも大丈夫-->
</ul>

<%= will_paginate %>